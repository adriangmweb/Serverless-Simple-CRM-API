{
  "name": "serverless-simple-crm-api",
  "version": "0.3.3",
  "description": "Simple CRM Serverless API",
  "scripts": {
    "sls-deploy": "npx serverless deploy -s",
    "deploy": "./.bin/prepareLayer.sh && npx serverless deploy -s",
    "prepare-layer": "./.bin/prepareLayer.sh",
    "local": "npx serverless invoke local -f",
    "test": "npx jest --forceExit --detectOpenHandles --verbose"
  },
  "keywords": [
    "nodejs",
    "serverless",
    "api"
  ],
  "author": "adrian gm",
  "license": "ISC",
  "devDependencies": {
    "aws-sdk": "^2.699.0",
    "jest": "^26.0.1",
    "sequelize-cli": "^5.5.1",
    "serverless": "^1.72.0",
    "serverless-apigw-binary": "^0.4.4",
    "serverless-dotenv-plugin": "^2.4.2"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "dotenv": "^8.2.0",
    "jsonwebtoken": "^8.5.1",
    "mysql2": "^2.1.0",
    "parse-multipart": "^1.0.4",
    "sequelize": "^5.21.12"
  }
}
