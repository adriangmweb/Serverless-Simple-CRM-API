{
  "name": "serverless-simple-crm-api",
  "version": "0.0.1",
  "description": "Simple CRM Serverless API",
  "scripts": {
    "deploy": "npx serverless deploy -s",
    "prepare-layer": "./.bin/prepareLayer.sh",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "nodejs",
    "serverless",
    "api"
  ],
  "author": "adrian gm",
  "license": "ISC",
  "devDependencies": {
    "serverless": "^1.72.0",
    "serverless-dotenv-plugin": "^2.4.2"
  }
}
